
def test_number_assumptions():
    age = 25

    # check to see if the age is a particular value
    assert age == 25

    # check to see if the age is over 18
    assert age > 18

    # check to see if the age is under 100
    assert age < 100